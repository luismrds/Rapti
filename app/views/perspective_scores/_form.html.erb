<%= form_for(@perspective_score) do |f| %>
  <% if @perspective_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@perspective_score.errors.count, "error") %> prohibited this perspective_score from being saved:</h2>

      <ul>
      <% @perspective_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :perspective_id %><br />
    <%= f.collection_select :perspective_id, Perspective.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :scoredate_id %><br />
    <%= f.collection_select :scoredate_id, ScoreDate.all, :id, :month %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.text_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
